# Canonical validation inputs

This directory retains only immutable regression input fixtures required by validation scripts. Rendered MP4s, previews, frame sequences, transient reports, and logs belong in the project-root `validation-evidence/` archive or are regenerated during validation.
